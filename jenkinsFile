pipeline {
    agent any
    
    stages {

        //
        stage("mvnw package") {
            steps {
                sh "sudo -s"
                sh "mvn package"
            }
        }
        
        //
        
        stage("java -jar target/*.jar") {
            steps {
                sh "java -jar target/*.jar"
            }
        }
    }
}
